# Starter Project

This repository contains a minimal example of a responsive Hero component that uses Tailwind CSS and Framer Motion for a fade-in animation.

The `Hero` component can be found in `src/components/Hero.tsx` and demonstrates how to combine Tailwind's utility classes with Framer Motion's animation API.
