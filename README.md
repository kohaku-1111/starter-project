# Starter Project

This project includes configuration for ESLint, Prettier, and Husky pre-commit hooks.

## Setup

1. Run `npm install` to install dependencies.
2. Run `npm run lint` to check code style.
3. Run `npm run format` to apply Prettier formatting.
4. Husky automatically runs `lint-staged` on commit.
